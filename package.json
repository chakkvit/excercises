{
  "name": "git-sdlc-exercise",
  "version": "0.1.0",
  "private": true,
  "description": "A hands-on git SDLC lab: every function you add shows up in a running local app.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node --test",
    "deploy": "node deploy.js",
    "status": "node deploy.js status",
    "stop": "node deploy.js stop",
    "logs": "node deploy.js logs",
    "rollback": "node deploy.js rollback",
    "setup": "git config core.hooksPath .githooks && node -e \"console.log('git hooks enabled')\""
  },
  "engines": { "node": ">=18" }
}
